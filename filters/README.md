# filters

Automated harness for the upstream `filters` lab.

Run from the repo root:

```bash
python3 filters/run.py
```

Refresh only the lab report from captured artifacts:

```bash
python3 filters/run.py --refresh-report
```

Generated outputs:

- `output/report.json`
- `output/report.html`
