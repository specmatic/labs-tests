# partial-examples

Automated harness for the upstream `partial-examples` lab.

Run from the repo root:

```bash
python3 partial-examples/run.py
```

Refresh only the lab report from captured artifacts:

```bash
python3 partial-examples/run.py --refresh-report
```

Generated outputs:

- `output/report.json`
- `output/report.html`
