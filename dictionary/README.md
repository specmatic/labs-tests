# dictionary

Automated harness for the upstream `dictionary` lab.

Run from the repo root:

```bash
python3 dictionary/run.py
```

Refresh only the lab report from captured artifacts:

```bash
python3 dictionary/run.py --refresh-report
```

Generated outputs:

- `output/report.json`
- `output/report.html`
